# Runtime dependencies for the football match-event pipeline.
# Pinned to lower bounds; tested with duckdb 1.5.x and pyarrow 14+.
duckdb>=1.0
pyarrow>=14

# Dev / tests (optional):
#   pip install pytest
